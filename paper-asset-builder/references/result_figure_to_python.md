# Result Figure to Python Reconstruction Reference

You are a Python developer proficient in data visualization, with expertise in using libraries such as Matplotlib, NetworkX, Seaborn, and others.

I have a plot generated by Python code, but I do not have the corresponding code that generated this plot. Your task is to generate Python code that can faithfully reproduce the picture based on the image I provide.

For this skill, the generated code should be inserted directly into the paper-level `README.md` under:

```markdown
### Plotting Code
```

The code should be complete, executable, and self-contained.

---

## 1. Data Extraction

Extract the actual data from the provided image.

Based on the visual features of the plot, infer the data and recreate the plot.

If exact values cannot be read from the image, estimate values that preserve:

- visible trend;
- ranking;
- scale;
- relative differences;
- qualitative conclusion.

Prioritize visual similarity to the reference image over producing generic or overly clean plotting code.

If exact values are not readable, add this sentence before the code block:

```markdown
Note: The following code is an approximate visual reconstruction based on the provided figure.
```

---

## 2. Recreate the Image

Generate Python code that reproduces the provided chart as faithfully as possible, matching both the chart semantics and the visible rendering details.

Reconstruct all important visual components, including:

- chart type and structure: scatter, line, bar, grouped or stacked bar, heatmap, multi-panel layout, inset axes, colorbars, twin axes, or mixed plots;
- canvas and layout: figure size, aspect ratio, background, plot area position, margins, subplot spacing, and panel labels;
- data geometry: bar heights and widths, line trends and marker positions, scatter distributions, error bars, confidence bands, shaded regions, heatmap colors, and reference lines;
- axes: axis ranges, scales, tick positions, tick labels, tick formatting, axis labels, titles, gridlines, and spine visibility;
- visual style: colors, colormaps, line widths, line styles, marker shapes, marker sizes, bar edges, hatches, transparency, and drawing order;
- text and annotations: font sizes, font weights, math symbols, legend labels and placement, annotation text, arrows, value labels, and significance markers.

Do not rely on default plotting styles when a visible style can be inferred from the image.

Explicitly set key parameters such as:

- `figsize`;
- `dpi` when saving;
- axis limits;
- ticks;
- labels;
- colors;
- fonts;
- line widths;
- marker sizes;
- legend position;
- grid style;
- spine style;
- margins.

If exact values or small text cannot be read, use the closest visually consistent approximation.

---

## 3. Self-contained Code

The Python code must be complete, executable, and self-contained.

All reconstructed data should be directly written into the Python code.

The code should not depend on external data files.

Recommended imports:

```python
import matplotlib.pyplot as plt
import numpy as np
```

Use extra libraries only when they are clearly necessary for faithful reproduction.

---

## 4. README Output Format

Use this block format inside the paper README:

````markdown
## Figure {index}: {figure_title}

<p align="center">
  <img src="result_figures/{image_filename}" alt="{figure_title}" width="720">
</p>

| Asset | Link |
|---|---|
| Preview Image | [{image_filename}](result_figures/{image_filename}) |

### Plotting Code

```python
...
```
````

---

## 5. Paper Asset Builder Integration

When used by `paper-asset-builder`, infer `{figure_title}` from both the image content and filename. The title should describe the chart type or finding, such as `Ablation Study`, `Accuracy-Latency Trade-off`, `Scaling Curve`, `Error Analysis`, or a more specific title visible in the figure.

Write only the README block content. Do not create separate `.py`, `.csv`, or data files unless the user explicitly asks for them.
